<h2 class="mb-3 h2-centered">Créez un gossip !</h2>
<div class="container-gossip-form">
  <div class="gossip-form">
  <%= form_tag url_for(action: 'create'), method: "post" do %>

    <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >

    <div class="form-group">
      <%= label_tag 'Titre' %>
      <%= text_field_tag 'title', @gossip.title, class: 'form-control', autofocus: true%>
      <small id="titleHelp" class="form-text text-muted">Entre 3 et 14 caractères.</small>
    </div>

    <div class="form-content">
        <%= label_tag 'Contenu' %>
        <%= text_area_tag 'content', @gossip.content, class: 'form-control', rows: 5 %>
    </div>

    <%= submit_tag "Publier", class: "button-gossip-form"%>
  <% end %>
  </div>
</div>

<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>